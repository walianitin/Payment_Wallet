"use client";
export default function tranfer(): JSX.Element {
  return (
   <div>
         {/* <Appbar onSignin={signIn} onSignout={signOut} user={session.data?.user} /> */}
         this is tranfer
   </div>
  );
}