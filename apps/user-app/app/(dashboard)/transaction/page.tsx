"use client";
export default function trancationPage(): JSX.Element {
  return (
   <div>
         {/* <Appbar onSignin={signIn} onSignout={signOut} user={session.data?.user} /> */}
         this is transactionPage
   </div>
  );
}